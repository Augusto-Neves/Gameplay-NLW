import * as discordAuth from "./discordAuth";

const scope = discordAuth.SCOPE;
const clientId = discordAuth.CLIENT_ID;
const redirectUri = discordAuth.REDIRECT_URI;
const responseType = discordAuth.RESPONSE_TYPE;
const cdnImage = discordAuth.CDN_IMAGE;

export { scope, clientId, redirectUri, responseType, cdnImage };
